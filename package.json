{
  "name": "nerdbeer-b0t",
  "version": "1.0.0",
  "description": "Nerbier Bot",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "$(npm bin)/static -a 0.0.0.0 -p $PORT public && node bot.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-static": "^0.7.9",
    "telegraf": "^3.6.5"
  },
  "engines": {
    "node": ">=6.2.0"
  }
}
